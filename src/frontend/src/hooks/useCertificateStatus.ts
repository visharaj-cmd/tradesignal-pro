// Stub — certificate system removed in FLASH USDT
// This file is kept to prevent broken imports from legacy code

export function useCertificateStatus() {
  return { hasCertificate: false, isLoading: false };
}
